# Gate 013 — Reversible Selection and Network Propagation

**Execution status:** PASSED  
**Identifiability result:** IDENTIFIABLE in the declared finite class  
**Decision:** Record a target-specific Gate 1 result only.

The audit distinguishes two deliberately separate target components:

\[
L_{RS}(f)=\left(R_{\mathrm{reopen}}(f),K_{\mathrm{flow}}(f)\right).
\]

A baseline current-performance readout is identical for all four systems. Each
single additional probe leaves a target-changing equivalence class:

- reversal recovery alone cannot distinguish independent from propagating
  influence at fixed reopenability;
- downstream flow alone cannot distinguish irreversible deletion from
  reversible selection at fixed flow.

The combined behavioral interface identifies the declared two-component target:

\[
O^*=\left(
\text{baseline},
\text{reversal recovery},
\text{downstream flow}
\right).
\]

Its scalar protocol cost is three because the frozen ladder mandates the
constant baseline control. Since that control contains no target information,
the informative projection \((\text{reversal recovery},\text{downstream
flow})\) identifies the target at informative cost two.

This factorization is constructive by design:

\[
\widehat L(1,r,k)=(r,k).
\]

It is an operational interface-composition result, not the discovery of an
adaptive-system invariant.

The complete finite audit, including every non-identifying witness, is in
[the result JSON](../results/gate_013_reversible_network_selection/factorization_audit.json).
It is deterministically regenerated by the audit script in this directory.

## Scope limit

This does **not** demonstrate an authority architecture, an online
self-diagnosis capability, predictive validity, or a beneficial intervention.
It only shows that this small operational target factors through the declared
behavioral intervention interface over the four-member deterministic class.